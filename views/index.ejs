<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <style >
        /* .card {
    box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
    transition: 0.3s;
    width: 150px;

  }
  
  .card:hover {
    box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
  }
  .img {
      max-height: 150px;
  }
  
  .container {
    padding: 2px 16px;
  } */
    </style>
</head>
<body>
    <form class="container" action="/" method="POST" enctype="multipart/form-data">
        <input type="text" name="companyName" placeholder="Nhập tên công ty"/>
        <input type="text" name="avatar" placeholder="Nhập link ảnh"/>
        <button type="submit">Lưu</button>
    </form>
    <% for(var i=0; i < data.length; i++) { %>
       
        <div class="card">
            <div><img src=<%=data[i].avatar %> alt="Avatar" style="width:100%"></div>
            
            <div class="container">
              <a href="/<%= data[i].companyName%>"><h3><b><%=data[i].companyName %></b></h3> </a>
              <form action="/delete" method="POST" enctype="multipart/form-data">
                <input value="<%=data[i].companyName%>" name="companyName"type="hidden"/>
                <button>Xoá</button>
              </form>
              
            </div>
          </div>
         
        
     <% } %>
</body>
</html>